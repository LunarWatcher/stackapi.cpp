/**
 * This file contains type definitions, because "integer" is not necessarily stonks in the long run. IDs could exceed ints,
 * and if/when that happens, this should be trivial to update.
 *
 * Ergo, API_INT.
 */

#define API_INT int
#define API_DATE long long

